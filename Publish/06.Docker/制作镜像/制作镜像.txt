#构建镜像

# docker login -u serset -p xxxxxxxxx
 

#把本文件所在目录下的文件夹拷贝到 /root/image 

cd /root/image
 
 
#构建镜像并推送到 镜像仓库
cd adaptor_gateway
docker build . -t serset/adaptor_gateway:1.1.354 -t serset/adaptor_gateway

 
docker push serset/adaptor_gateway:1.1.354
docker push serset/adaptor_gateway





#强制删除镜像名称中包含adaptor_gateway的镜像
# docker rmi --force $(docker images | grep adaptor_gateway | awk '{print $3}')










 