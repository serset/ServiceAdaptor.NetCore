#构建镜像

# docker login -u serset -p xxxxxxxxx
 

#把本文件所在目录下的文件夹拷贝到image 

cd /root/image
cd adaptor_gateway
 

#构建镜像
docker build . -t serset/adaptor_gateway:1.1.372 -t serset/adaptor_gateway


#送到镜像仓库
docker push serset/adaptor_gateway:1.1.372
docker push serset/adaptor_gateway





#强制删除镜像名称中包含adaptor_gateway的镜像
# docker rmi --force $(docker images | grep adaptor_gateway | awk '{print $3}')










 